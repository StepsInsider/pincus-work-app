name: pincus_work
description: "Pincus Work ERP System"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.3
  image_picker: ^1.1.2
  flutter_riverpod: ^3.3.2
  go_router: ^14.1.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  get_it: ^9.2.1
  shared_preferences: ^2.5.5
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.6.0
  freezed: ^3.2.5
  json_serializable: ^6.10.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png
